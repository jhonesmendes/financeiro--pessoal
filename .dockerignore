**/node_modules
.lage
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Never bake real user data, secrets, or dev-only scratch files into an image
packages/sync-server/server-files
packages/sync-server/user-files
.env
.env.*
.tmp

# Scratch working directory with a nested .git that confuses lage's git-based
# hashing; not needed to build the app
work
